// Difficulty: 1.3

#include <stdio.h>
#include <stdbool.h>

bool hasVisited(char** ){
    return false;
}

int main(){
    char visited[100][20];
    int numTrips,numStops;
    scanf("%d",&numTrips);
    for(int i = 0; i < numTrips; ++i){
        scanf("%d",&numStops);
        if(hasVisited(&visited[0][0])){

        }
    }
}
